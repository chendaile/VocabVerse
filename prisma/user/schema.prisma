generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/@prisma/user-client"
}

datasource db {
  provider = "postgresql"
}

model user{
        id      String @id @default(cuid())
        
}