# VocabVerse 后端（NestJS + Prisma）

- 技术栈：NestJS 11、Prisma、PostgreSQL。
- 模块化：当前包含 `words` 示例模块，后续可扩展 `auth / user / progress / review`。
- 配置：复制 `.env.example` 为 `.env` 并补充数据库链接。
- 运行：安装依赖后使用 `npm run start:dev` 启动开发服务。
